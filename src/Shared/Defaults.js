/**
 * Plugin constants (hardcoded configuration) for shared code.
 */
export default class Wallet_Shared_Defaults {
    NAME = '@flancer64/pwa-wallet';

    SPACE_PROXY = 'proxy';

    constructor() {
        Object.freeze(this);
    }
}
